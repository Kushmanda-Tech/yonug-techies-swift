 var fromNumber = 21
 var  toNumber = 30
 
 var NumberForTable = fromNumber
 
 for NumberForTable in (fromNumber...toNumber){
    
 
 for counter in (1...10)
 {
    print(" the answer of \(NumberForTable) Multiplied by \(counter) is \(counter*NumberForTable)")
    counter == counter+1
 };
    NumberForTable == NumberForTable+1
 };
